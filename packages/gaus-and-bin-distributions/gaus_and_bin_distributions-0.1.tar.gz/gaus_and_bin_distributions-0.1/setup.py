from setuptools import setup

setup(name='gaus_and_bin_distributions',
      version='0.1',
      description='Gaussian and binomial distributions',
      packages=['gaus_and_bin_distributions'],
      zip_safe=False)
