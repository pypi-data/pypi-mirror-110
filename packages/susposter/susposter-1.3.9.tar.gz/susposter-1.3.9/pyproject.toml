[tool.poetry]
name = "susposter"
version = "1.3.9"
description = "so sussy"
repository = "https://github.com/Cryptex-github"
authors = ["Cryptex"]

[tool.poetry.dependencies]
python = "^3.6"

[build-system]
requires = ["poetry_core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
