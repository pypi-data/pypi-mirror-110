from setuptools import setup

setup(name='bdr_spl_distributions',
      version='0.1',
      description='Gaussian distributions',
      packages=['bdr_spl_distributions'],
      author = 'bdr spl',
      zip_safe=False)
